# Tooling

- Python: always use `uv run` to run scripts (e.g. `uv run main.py`)
- Helm: read chart values with `helm show values`
- Use dedicated tools (Read, Glob, Grep, Edit, Write) instead of shell equivalents (cat, find, grep, sed, awk)
